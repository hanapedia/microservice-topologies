db = new Mongo().getDB("{{ name }}")
db.createCollection("{{ type }}{{ x }}", { capped: false })
let entries = []
for (let i = 0; i <= 50; i++){
  entries.push({key: i, value: Math.floor(Math.random() * 50)})
}
db["{{ type }}{{ x }}"].insertMany(entries)
